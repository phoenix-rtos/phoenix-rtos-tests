test:
    type: unit
    tests:
        - name: unit
          exec: test-libc
          #FIXME: symlink tests will fail on non-rootfs targets
          targets:
              exclude: [armv7m7-imxrt106x]

