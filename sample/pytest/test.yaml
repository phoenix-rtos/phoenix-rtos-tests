test:
    tests:
        - name: pytest
          type: pytest
          execute: test-fake-comm
          script: test-pytest.py
          targets:
              include: [host-generic-pc]

        - name: test_psh
          type: pytest
          script: test-psh.py
