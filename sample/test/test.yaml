test:
    tests:
        - name: echo
          harness: test-echo.py
          execute: test-echo
          targets:
              include: [host-generic-pc]

        - name: unity
          execute: test-unity
          type: unity
          targets:
              include: [host-generic-pc]

        - name: pytest
          type: pytest
          kwargs:
            path: phoenix-rtos-tests/sample/test/test-pytest.py
          targets:
              include: [host-generic-pc]
