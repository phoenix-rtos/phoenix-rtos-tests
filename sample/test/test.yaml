test:
    tests:
        - name: echo
          exec: test-echo
          harness: test-echo.py
          targets:
              include: [host-pc, armv7m7-imxrt106x]

        - name: unity
          exec: test-unity
          type: unit
          targets:
              include: [host-pc, armv7m7-imxrt106x]

