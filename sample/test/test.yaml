test:
    tests:
        - name: echo
          exec: test-echo
          harness: test-echo.py
          targets:
              include: [host-generic-pc]

        - name: unity
          exec: test-unity
          type: unit
          targets:
              include: [host-generic-pc]
