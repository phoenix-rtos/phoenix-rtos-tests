test:
    tests:
        - name: echo
          harness: test-echo.py
          execute: test-echo
          targets:
              include: [host-generic-pc]

        - name: unity
          execute: test-unity
          type: unity
          targets:
              include: [host-generic-pc]
