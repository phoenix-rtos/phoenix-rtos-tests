# Minimal test config for TestTarget.test_empty_targets

test:
    exec: fake-exec

    targets:
        value: [host-pc, ia32-generic]

    tests:
        - name: test_0
          targets:
              exclude: [host-pc, ia32-generic]
